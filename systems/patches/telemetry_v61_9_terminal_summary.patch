--- a/systems/telemetry.lua
+++ b/systems/telemetry.lua
@@ -403,9 +403,12 @@
             )
         end
 
-        writeMatchSummary(
-            "RUNNING"
-        )
+        -- Once settlement is authoritative, later heartbeat/event appends
+        -- must never overwrite the permanent terminal summary as RUNNING.
+        if not SettlementArchived then
+            writeMatchSummary(
+                "RUNNING"
+            )
+        end
     end
 
     local function counts()
