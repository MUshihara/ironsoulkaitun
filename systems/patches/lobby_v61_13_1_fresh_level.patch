--- a/systems/lobby.lua
+++ b/systems/lobby.lua
@@
 local loaded =
     waitUntil(
         function()
             local data =
                 pdata()
@@
-            if LocalPlayer:
-                    GetAttribute(
-                        "LG_Level"
-                    )
-                == nil
-                or LocalPlayer:
-                    GetAttribute(
-                        "LG_PowerNew1"
-                    )
-                == nil
+            -- Fresh accounts can have authoritative PlayerData.LevelData.Level
+            -- while the historical LG_Level mirror attribute is never created.
+            -- Treat PlayerData as the primary fallback instead of waiting forever
+            -- for a client attribute that may legitimately remain nil.
+            local resolvedLevel =
+                tonumber(
+                    LocalPlayer:
+                        GetAttribute(
+                            "LG_Level"
+                        )
+                )
+                or (
+                    type(data.LevelData) == "table"
+                    and tonumber(data.LevelData.Level)
+                )
+
+            if resolvedLevel == nil
+                or LocalPlayer:
+                    GetAttribute(
+                        "LG_PowerNew1"
+                    )
+                == nil
             then
                 return nil
             end
@@
 local function level()
-    return num(
-        LocalPlayer:
-            GetAttribute(
-                "LG_Level"
-            )
-    )
+    local d = pdata()
+
+    return num(
+        LocalPlayer:
+            GetAttribute(
+                "LG_Level"
+            )
+        or (
+            d
+            and type(d.LevelData) == "table"
+            and d.LevelData.Level
+        )
+    )
 end
