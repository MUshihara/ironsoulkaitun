--- a/systems/lobby.lua
+++ b/systems/lobby.lua
@@ -128,8 +128,17 @@
 local function important(s)
     print(
         "[IronSoul]",
         tostring(s)
     )
+
+    local status =
+        getgenv().IronSoulStatus
+
+    if type(status) == "function" then
+        pcall(
+            status,
+            "Lobby | " .. tostring(s)
+        )
+    end
 end
 
 local function shouldPrintLobby(text)
@@ -342,28 +351,21 @@
         )
     )
 end
 
-local queue =
-    queue_on_teleport
-    or (
-        syn
-        and syn.queue_on_teleport
-    )
-
-if type(queue)
-        ~= "function"
-then
-    return stop(
-        "V59 requires queue_on_teleport for continuous progression."
-    )
-end
-
 local queueBootstrap =
     getgenv().IronSoulQueueBootstrap
 
 if type(queueBootstrap)
         ~= "function"
 then
     return stop(
-        "V59 bootstrap queue helper missing."
+        "V61.10 bootstrap queue helper missing."
     )
 end
+
+local queueAvailable =
+    getgenv().IronSoulTeleportQueueAvailable
+        == true
+
+important(
+    "Mobile compatibility | teleport queue="
+        .. tostring(queueAvailable)
+)
 
 local JOURNAL_FILE =
     "IronSoul_Kaitun_Journal_V59.txt"
@@ -4640,16 +4642,23 @@
 local queued =
     queueBootstrap(
         "lobby -> dungeon"
     )
 
 if not queued then
-    return stop(
-        "Could not queue V59 bootstrap before dungeon teleport."
+    important(
+        "Teleport queue unavailable | continuing current cycle; "
+            .. "re-execute loader once after dungeon teleport if executor "
+            .. "does not auto-run it."
     )
+else
+    matchLog(
+        "V61.10 bootstrap queued."
+    )
 end
 
-matchLog(
-    "V59 bootstrap queued."
-)
-
 --========================================================--
 -- SELECT + CREATE SOLO STORY
 --========================================================--
